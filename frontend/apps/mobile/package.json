{
  "name": "@lightwick/mobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm -C ../web exec vite build --mode app",
    "sync": "pnpm build && cap sync",
    "ios": "pnpm sync && cap open ios",
    "android": "pnpm sync && cap open android",
    "check": "tsc -p ."
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.0"
  }
}
