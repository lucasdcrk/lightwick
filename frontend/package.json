{
  "name": "lightwick-frontend",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/web build",
    "check": "pnpm -r check"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
