services:
  ha:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: lightwick-ha
    ports: ["8123:8123"]
    environment:
      TZ: Europe/Paris
    volumes:
      - ./config:/config
      - ../custom_components/lightwick:/config/custom_components/lightwick
    restart: unless-stopped
